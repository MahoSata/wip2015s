#ifndef STORAGE_VOLUMES_H
#define STORAGE_VOLUMES_H

enum {
  VOLUME_BLOCKDEV, 
  VOLUME_LOGGING, 
  VOLUME_CONFIGKEY, 
};

#endif
#if defined(VS)
VS(VOLUME_BLOCKDEV, 512)
VS(VOLUME_LOGGING, 1024)
VS(VOLUME_CONFIGKEY, 512)
#undef VS
#endif
#if defined(VB)
VB(VOLUME_BLOCKDEV, 0)
VB(VOLUME_LOGGING, 512)
VB(VOLUME_CONFIGKEY, 1536)
#undef VB
#endif
